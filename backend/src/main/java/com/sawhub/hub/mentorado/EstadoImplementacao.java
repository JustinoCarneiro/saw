package com.sawhub.hub.mentorado;

/** M23 — três estados (não booleano) pra "cultura construída"/"processos desenhados" no
 * Diagnóstico Inicial — o Notion real da operação usa "em construção" como resposta comum,
 * distinto de sim/não puro. */
public enum EstadoImplementacao {
    SIM,
    NAO,
    EM_CONSTRUCAO
}
