node_modules/
.DS_Store
.thumbnail
target/
dist/
build/
*.class
*.jar
*.war
.env

# Arquivos de IDE
.idea/
.vscode/
*.swp

# Scripts de dev (logs/PIDs do dev-up.sh / dev-down.sh)
.dev/

# Áudio de ata (M06) salvo em disco local no dev — AUDIO_STORAGE_DIR aponta pra cá por padrão
backend/data/

# Exports de planilhas/Notion do cliente (dado de mentorado, sigiloso) — nunca versionar
dados-cliente-notion/

# Arquivos locais com dado de conta/PII (e-mail pessoal, custos, nomes de outros clientes da
# Onda) — CLAUDE.md sempre disse "arquivo local, não versionado", mas estavam versionados de
# fato (achado da revisão final de segurança da Fase 5, 20/07/2026). git rm --cached só impede
# rastreamento novo; o conteúdo continua nos commits antigos já publicados até uma limpeza de
# histórico separada.
transicao-acessos-para-cliente.md
custos-operacionais.md
